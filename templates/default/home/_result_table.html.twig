<table class="table table-striped table-responsive-sm">
	<thead>
	<tr>
		<th scope="col">Équipe de Forges</th>
		<th scope="col">Adversaire</th>
		<th scope="col">Victoire</th>
		<th scope="col">Score</th>
	</tr>
	</thead>
	<tbody>
	{% for r in result %}
		<tr>
			<td scope="row">{{ r.team.name }}</td>
			<td>{{ r.opponent }}</td>
			<td>{{ r.winner }}</td>
			<td>{{ r.winnerScore }}</td>
		</tr>
	{% endfor %}
	</tbody>
</table>