{{ form_start(form) }}
    {{ form_row(form.name) }}
{% include "administration/gallery/_image_widget.html.twig" %}
<button class="btn" type="submit">{{ button_label|default('Save') }}</button>
{% do form.images.setRendered %}
{{ form_end(form) }}