{% extends 'administration/admin.html.twig' %}

{% block title %}Gestion des slides{% endblock %}
{% block header %}Slide {{ slide.id }}{% endblock %}

{% block body %}
    <table class="table table-responsive-sm table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ slide.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ slide.name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ slide.slug }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>
                    <img src="{{ asset('uploads/slides/' ~ slide.image) }}" alt="" height="300">
                </td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('slide_index') }}">back to list</a>

    <a href="{{ path('slide_edit', {'id': slide.id}) }}">edit</a>

    {{ include('administration/slide/_delete_form.html.twig') }}
{% endblock %}
