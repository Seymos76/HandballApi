{% extends 'base.html.twig' %}

{% block title %}Administration{% endblock %}

{% block body %}
	{% if is_granted('ROLE_USER') %}
	<a href="{{ path('game_index') }}">Les matchs</a>
	<a href="{{ path('team_index') }}">Les équipes</a>
	<a href="{{ path('user_index') }}">Les utilisateurs</a>
	<a href="{{ path('player_index') }}">Les joueurs</a>
	<a href="{{ path('image_index') }}">Les images</a>
	<a href="{{ path('gallery_index') }}">Les galeries</a>
	<a href="{{ path('slide_index') }}">Les slides</a>
	<a href="{{ path('training_index') }}">Les entraînements</a>
	<a href="{{ path('article_index') }}">Les articles</a>
	<a href="{{ path('password_change') }}">Changer le mot de passe</a>
	<a href="{{ path('logout') }}">Déconnexion</a>
	{% endif %}
	{% if app.user %}
		{{ dump(app.user) }}
	{% endif %}
{% endblock %}
