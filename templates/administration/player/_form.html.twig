{{ form_start(form) }}
{{ form_row(form.firstname) }}
	{{ form_row(form.lastname) }}
	{{ form_row(form.position) }}
	{{ form_row(form.team) }}
	{% if player is defined and player.image is not null %}
		<img src="{{ asset('uploads/players/' ~ player.image) }}" alt="">
	{% endif %}
	{{ form_row(form.image) }}
    <button class="btn btn-outline-primary">{{ button_label|default('Ajouter') }}</button>
{{ form_end(form) }}
